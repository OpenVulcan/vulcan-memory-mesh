# 任务计划：补充标准构建与运行规则说明

## 任务目标

本次任务目标是在 VMM 仓库中补充一条更明确的通用说明，让 AI 与协作者都清楚：

1. 标准构建方式应使用 `.\make.bat build` 或 `.\make.bat build release`。
2. 不应绕过打包脚本直接使用 `go build` 之类的临时构建方式。
3. 正式运行时应进入 `output/bin/` 目录并从其中的可执行文件启动。

## 执行步骤

1. 检查 `AGENTS.md` 与 `README.md` 中现有的构建/运行说明位置。
2. 在 `AGENTS.md` 中补充更强约束的标准 build/run 规则。
3. 在 `README.md` 中补充面向使用者的运行说明，明确从 `output/bin` 启动。
4. 检查文案一致性，补执行变更总结并归档计划文件。

## 技术选型与处理原则

- 优先把规则写入 `AGENTS.md`，因为这是 AI 最容易优先读取的仓库规则文件。
- 同时在 `README.md` 中保留用户可见说明，避免规则只对 AI 可见。
- 文案要明确禁止临时产物作为正式运行入口。

## 验收标准

1. `AGENTS.md` 已明确标准构建命令与运行目录。
2. `README.md` 已同步说明正式运行方式。
3. 当前计划文件已完成执行总结并归档。

## 执行变更总结

### 1. 核心修复与调整概述

本次调整补充了仓库级的标准构建与运行说明，明确要求后续协作者与 AI 默认使用 `.\make.bat build` 或 `.\make.bat build release` 作为标准构建入口，并且从 `output/bin/` 目录中的正式产物启动程序，避免再次把根目录临时产物误判为可运行交付物。

### 2. 📂文件变更清单

新增：
- `docs/plan/20260418-04-add-build-run-rules.md`

修改：
- `AGENTS.md`
- `README.md`

删除：
- 无

### 3. 💻关键代码调整详情

- 在 `AGENTS.md` 的构建规则中补充：
  - `.\make.ps1 build release`
  - `.\make.bat build release`
- 在 `AGENTS.md` 中新增“运行规则”章节，明确：
  - 必须使用 `output/bin/` 中的可执行文件
  - 手工启动时先进入 `output/bin/`
  - 不要从仓库根目录直接运行临时产物
- 在 `README.md` 顶部补充“标准构建与运行”章节，给开发者和 AI 提供同样清晰的可见说明。

### 4. ⚠️遗留问题与注意事项

- 本次仅为仓库规则与说明文档调整，不涉及运行时代码逻辑变更。
- 因为没有改动功能代码，本次未额外执行测试命令。
