# 任务计划：提升 vldb-lancedb 版本并打标推送

## 任务目标

本次任务目标是在 `D:\projects\VulcanLocalDataGateway\vldb-lancedb` 仓库中为刚完成的 FFI runtime 修复提升版本号、创建对应 git tag，并推送到远端，确保发布标识与代码状态一致。

## 执行步骤

1. 确认当前仓库版本号、已有 tag 和最近提交状态，选择最小安全递增版本。
2. 修改版本号相关文件，检查是否有需要同步的文档或示例说明。
3. 使用中文结构化提交信息提交版本调整。
4. 创建对应版本 tag 并推送 commit 与 tag。
5. 在当前仓库补执行总结并归档计划文件。

## 技术选型与处理原则

- 采用最小递增版本策略，不做无必要的大版本跳变。
- 版本号、提交、tag 三者保持一致。
- 如仓库中已有同名 tag，不强行覆盖，优先选择下一个可用版本。

## 验收标准

1. `vldb-lancedb` 仓库版本号已更新。
2. 版本变更已提交并推送。
3. 对应 tag 已创建并推送。
4. 当前计划文件已完成执行总结并归档。

## 执行变更总结

### 1. 核心修复与调整概述

本次将 `vldb-lancedb` 仓库版本从 `0.1.4` 提升到 `0.1.5`，用于承接上一轮 FFI runtime `EnterGuard` 修复后的发布标识；同时完成版本提交、tag 创建和远端推送，确保代码、版本号和发布标签保持一致。

### 2. 📂文件变更清单

新增：
- 无

修改：
- `D:\projects\VulcanLocalDataGateway\vldb-lancedb\Cargo.toml`
- `D:\projects\VulcanLocalDataGateway\vldb-lancedb\Cargo.lock`

删除：
- 无

### 3. 💻关键代码调整详情

- 将 `Cargo.toml` 中的包版本由 `0.1.4` 提升到 `0.1.5`。
- 同步更新 `Cargo.lock` 中本包条目的版本号，避免发布元信息不一致。
- 使用中文结构化提交信息完成版本提交：
  - 提交号：`c0a78ef`
  - 标题：`提升版本号到 0.1.5 并同步发布元信息`
- 创建并推送 git tag：
  - `v0.1.5`

### 4. ⚠️遗留问题与注意事项

- `cargo check` 已通过，说明版本元信息调整未破坏仓库构建状态。
- 当前远端 `origin/main` 已包含版本提交，`origin/v0.1.5` tag 也已创建完成。
- 这次仅是版本标识与发布 tag 调整，不涉及额外行为变更。
