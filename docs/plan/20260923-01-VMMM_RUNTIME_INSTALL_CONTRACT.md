# VMMM 安装器所需 VMM 运行时与发行契约

状态：实施中。

## 任务目标

为独立 VMMM 安装器提供可验证、可配置、可回滚的 VMM 正式包契约。保留旧配置的运行行为，避免把仓库开发配置、测试凭据或不完整的存储依赖带入发行包。

## 执行步骤

1. 对照现有配置加载、存储、服务和发行代码确认权威字段及目录布局。
2. 在 split/controller 模式增加显式本地数据根字段，空值沿用旧路径；阻止模式切换时静默迁移旧数据库。
3. 增加配置 schema、完整配置与规则资产校验、健康探测命令，供管理器在安装前与运行后调用。
4. 让服务注册携带显式配置根，并支持重启、自启启停和状态查询。
5. 将每平台发行包改为同时包含 native、split、controller 所需运行依赖，严格列举可打包配置文件，排除开发覆盖配置。
6. 使用独立于 Windows Certum 代码签名的 Ed25519 清单签名；私钥仅存 GitHub Actions Secrets，固定公钥随仓库提交；缺密钥或验签失败时不发布。
7. 同步 README 与发行指南，验证测试、标准构建和目标平台产物。

## 技术选型

- 保持现有 Go 配置模型、YAML 配置链和 VMM CLI 为最终权威。
- 外部发行清单采用 Ed25519 分离签名，签名实现使用标准库；Certum 代码签名待证书签发后接入。
- 每个平台一个完整包，包内收据声明实际文件摘要和存储能力。
- 继续使用现有服务管理和打包脚本，仅扩充明确协议。

## 验收标准

- 旧 split/controller 配置仍使用旧数据库路径，新字段在对应模式生效且路径冲突安全拒绝。
- schema 与 validate 反映真实配置字段；校验失败不回显凭据。
- 服务模式和命令行模式读取同一显式配置根，健康探测使用现有 gRPC 契约。
- 五个平台的包都包含三类本地依赖，且不包含开发用 `configs/config.yaml`。
- 正式清单按固定公钥验证；缺失 Secret 或摘要不符失败关闭。
- 聚焦测试、`go test ./...`、标准构建和发行包测试通过；跨平台交叉编译与原生运行证据分别记录。

## 执行变更总结

待全部验收后填写核心调整、文件清单、关键代码和遗留事项，再迁移至 `docs/completed/20260923/01-VMMM_RUNTIME_INSTALL_CONTRACT.md`。

## 阶段验收记录

- 供应商在线测试补齐计划：已有配置 CLI 增加显式允许网络的测试动作；沿用真实分层加载和已有 LLM、embedding、rerank 工厂，仅发送固定测试内容，不创建存储或运行日志。用途与路由下标须验证，总时限二十秒；固定结果码独立于静态校验，覆盖本地模拟服务和未经确认零请求回归。
- 供应商在线测试已实现，本机按 `go.mod` 对应的 Go 1.26.1 完成全量一千二百九十九项测试与静态检查。回环服务覆盖真实 LLM、embedding、重排请求、错误脱敏、维度异常、取消和确认门禁；完整命令链覆盖所选候选配置生效。未调用实际收费端点，新的五平台运行待验证。
- 有效配置导出提交 `ece45f0` 的运行 `35874981009` 已在五个平台全部通过，包括实际打包程序的配置导出烟测。

- 有效配置展示修复计划：扩展已有配置检查入口，提供只读 `config show-effective`，复用 `ResolvePromptLayout` 与 `LoadPaths`，按现有 schema 敏感路径规则省略凭据；不启动数据库或供应商请求。新增分层、环境覆盖、脱敏和错误输出回归，并运行配置及全量测试。管理器直接消费此版本化结果，不再将用户覆盖文件称为有效配置。

### 生效配置来源执行记录

1. 核心调整：在既有加载链增加可选只读观察，记录实际文件合并、环境覆盖及归一化的逐叶子来源，普通启动入口不启用跟踪。导出第二版协议，保留敏感字段省略策略及真实空值。
2. 文件变更：新增来源观察与回归测试；修改配置加载、共享安全投影、导出协议、命令入口及其测试、仓库说明和本计划。
3. 关键逻辑：来源使用规范 JSON Pointer，区分同值覆盖、标量空值被忽略、可空字段赋空、集合项删除、动态参数键和环境展开。归一化改变显式输入时保留输入来源并标记调整；不将配置中的空值冒充最终生效空值。
4. 验证及边界：配置和命令入口一百六十一项测试通过；此前全量一千三百零二项通过，新增动态参数引用回归后须再次全量验证。原工作目录标准构建因既有非受管配置拒绝覆盖；隔离工作树使用相同标准构建入口和重新验签摘要的依赖档案完成构建。未修改原有配置，未连接数据库或供应商。正式 Certum 发行门禁保持。

最新验证：动态参数环境引用采用原始配置树的 JSON Pointer，已增加扁平点号键与嵌套键区分回归。完整一千三百零三项测试与静态检查通过，更新后的隔离标准构建成功。五平台持续集成等待提交后验证。

- 新增：`internal/config/effective_sources.go`、`internal/config/effective_sources_test.go`。
- 修改：`internal/config/config_load.go`、`internal/config/schema.go`、`internal/config/effective.go`、`cmd/vmm-local/config_effective.go`、`cmd/vmm-local/config_effective_test.go`、`README.md`、本计划。
- 删除：无。
- 有效配置导出已落地，配置与命令入口聚焦一百五十五项测试、全量一千二百九十二项测试及静态检查通过。原生烟测已加入实际标准包的有效配置、默认值和密钥省略断言；待新提交的五平台持续集成确认。说明已同步 README，处理与噪声指南不受此只读诊断命令影响。

- VMM PR 的五平台原生服务管理器测试已在 Windows x64、Linux x64/ARM64、macOS Intel/ARM64 上通过安装、状态、自启启停、启动、健康探测、重启、停止和卸载流程；Linux 独立临时服务账户的追加验收仍待后续 CI 运行。
- 本机 Windows 对已有原生库缓存执行 ABI 与错误边界、真实写入检索、未完成 Schema 恢复三项测试，均通过。该结果不代表其他平台或最终完整发行包通过。
- PR 工作流已加入五平台原生库编译、`all` 配置完整包构建及不允许跳过的打包运行时测试；首次 Rust 编译仍在运行，不能据此宣布完整包验收通过。
- Certum Windows 代码签名证书和公开发行验收仍未到位，故计划继续留在实施中。
- 五平台完整包首轮原生 CI 中，Linux x64/ARM64 的原生 ABI、读写和恢复测试通过；split/controller 打包子测试因 Go `t.TempDir()` 子目录权限受 umask 影响为 0755，被运行时 0700 安全校验正确拒绝。已改为在测试夹具中显式创建并校验私有数据根，不降低运行时权限要求；待五平台缓存就绪后复跑。
- 第二轮完整包的 Linux x64/ARM64、macOS ARM64 已通过原生及 split/controller 打包运行验收。Linux 以独立本机账户启动系统服务时暴露真实问题：旧日志默认写在管理员持有的程序包同级目录。新增可选 `logging.directory` 绝对路径，旧配置保持原路径；管理器两种运行模式均将其指向数据根下的 `logs`，VMM 服务注册前检查可创建性与归属。修复后的独立账户烟测待下一轮 CI 验证。
- 修复日志路径后 Linux 独立账户服务已成功运行并监听 gRPC；烟测的普通 CI 账户无权读取独立账户私有配置根，导致健康探测返回配置校验错误。烟测现以与特权管理器相同的权限执行健康检查；服务运行时配置权限保持 0700。
- 提交 `efd6027` 对应的运行 `35855247408` 五平台全部通过：Windows x64、Linux x64/ARM64、macOS Intel/ARM64 均完成完整包构建与原生存储 ABI、真实写入检索、恢复、split/controller 打包运行及系统服务验收；Linux 独立本机账户服务烟测也通过。该记录取代前述待运行状态，尚不代表正式签名公开发行或干净机器引导安装已经完成。
- 为支持服务注册中断后直接重装，新增精确的服务不存在状态与已确认不存在时的幂等卸载。Linux 核对文件和加载状态、macOS 核对属性列表和已加载服务、Windows 只接受系统不存在错误码；权限和同名外部服务错误继续拒绝。原生烟测加入安装前不存在、卸载后不存在和重复卸载检查；本机服务入口三十二项测试通过，最新跨平台原生结果待持续集成。
- 服务不存在协议已在 Linux 双架构与 macOS 双架构原生服务烟测通过。Windows 持续集成在原有 SQLite 功能夹具初始化处触发一秒超时，尚未运行新服务烟测；该夹具用于功能而非延迟验收，现改用生产默认五秒超时，保留取消行为和运行时超时策略。本机该包十三项测试通过，待新提交复验 Windows。
- 提交 `f2394c4` 对应运行 `35872011361` 已在五个平台全部通过，包括 Windows 的完整包、原生存储与系统服务检查。新增的安装前服务不存在、卸载后不存在、重复卸载均由原生服务烟测验收；上述待复验状态已解除。公开签名发行及干净机器引导安装仍未完成。
- 提交 `1e2e3cd` 的五平台运行 `35886159893` 在标准构建、配置测试及打包存储验收通过后，均被服务烟测中仍要求 `show-effective` 第一版协议的旧断言阻断，未运行后续服务步骤。烟测现要求真实第二版结果，检查脱敏、用户覆盖字段的精确来源以及系统底座字段来源；本机使用隔离标准包复现第二版输出和三处来源，Python 语法检查通过。待新五平台运行重新证明完整服务链。

### 发行草稿门禁复审

1. 核心调整：复审发现 VMM 发行脚本在草稿资产下载核验之后仍会执行 `gh release edit --draft=false`。根据用户确认的 Certum 待签发决定，取消自动公开动作，发行任务只创建和核验草稿；最终连续无问题审核计数归零。
2. 文件变更：修改 `scripts/release.py`、`scripts/test_release.py`、`.github/workflows/release.yml`、`README.md`、`docs/github-release-guide_CN.md`、`docs/release-public-keys.md` 与本计划；无新增或删除文件。
3. 关键逻辑：发行 CLI 改为显式 `draft` 操作，仍先验收五个平台、签名清单和 Certum 证明，再上传草稿、重新下载核对摘要并确认远端仍为草稿；不提供公开发行命令。回归测试模拟 GitHub 创建与下载，断言创建带 `--draft`、不调用编辑公开，并在远端状态异常时失败。
4. 验证与边界：草稿专项及发行库共十一项 Python 测试、全量一千三百零三项 Go 测试、`go vet ./...`、发行 CLI 参数检查及差异检查通过。本次仅调整发行控制而未修改运行时打包路径，既有标准构建结果仍有效；新提交的五平台持续集成待复验。证书仍在办理，实际 Certum 签名资产与公开下载验收未完成，计划继续处于实施状态。

### 发行工作流说明复审

1. 核心修复与调整概述：发行工作流文件头仍写“发布完整版本”，与已实行的 Certum 草稿门禁不一致；改为明确只创建并核验五平台草稿，连续无问题审核计数重新归零。
2. 📂文件变更清单：修改 `.github/workflows/release.yml` 与本计划；无新增或删除文件。
3. 💻关键代码调整详情：仅修正工作流英文及中文说明，不更改实际构建、签名、上传和草稿校验步骤；`scripts/release.py draft` 仍是唯一入口。
4. ⚠️遗留问题与注意事项：VMM 发行专项十一项 Python 测试通过；既有 VMM 五平台 CI 通过，新增注释提交待复验。Certum 证书尚未到位，正式发行门禁继续保留。
